spring:
  application:
    name: msvc-users

  cloud:
    config:
      enabled: true
      uri: http://msvc-config.inventario-ms.local:8081
      fail-fast: false
      retry:
        max-attempts: 30
        initial-interval: 2000
        multiplier: 1.5
        max-interval: 15000