spring:
  application:
    name: msvc-pay
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
      fail-fast: false
      retry:
        max-attempts: 30
        initial-interval: 2000
        multiplier: 1.5
        max-interval: 15000