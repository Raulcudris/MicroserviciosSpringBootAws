spring:
  application:
    name: msvc-orders
    
  cloud:
    config:
      enabled: true
      uri: ${SPRING_CLOUD_CONFIG_URI:http://configservice.microservices-fargate-demo.local:8081}
      fail-fast: ${SPRING_CLOUD_CONFIG_FAIL_FAST:false}
